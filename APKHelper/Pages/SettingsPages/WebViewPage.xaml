<Page
    x:Class="APKHelper.Pages.SettingsPages.WebViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:APKHelper.Pages.SettingsPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helper="using:APKHelper.Helpers"
    xmlns:controls="using:APKHelper.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border  Grid.Row="1" >
            <WebView2 x:Name="mainWebView"  Source="http://1.qq.com/yingyongbao/" Margin="5"/>
        </Border>
        <TextBox></TextBox>
        <controls:TitleBar
            x:Name="TitleBar" 
            x:Uid="/SettingsPage/Promote"
            BackRequested="TitleBar_BackRequested"
            BackgroundVisibility="Visible"
            IsBackEnable="{x:Bind Frame.CanGoBack}" />
    </Grid>
</Page>
